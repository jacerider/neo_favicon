{% extends 'neo-toolbar-element.html.twig' %}

{%
  set image_size = {
    width: 36,
    height: 36,
  }
%}
{# {% block image %}
  {{ neo_image_style(image, {cropSides: 1, scale: {width: 36, height: 36}}) }}
{% endblock %} #}
